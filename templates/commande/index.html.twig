{% extends 'base_BACK.html.twig' %}
    {% block body %}

        <center><h1>Les commandes</h1></center>
        <table class="table">
            <thead>
            <tr>
                
                <th>Quantité</th>
                <th>IdCommande</th>
                <th>Nom du Produit</th>
                <th>Price</th>
                

            </tr>
            </thead>
            <tbody>
            {% for commandes in pagination %}
                <tr>
                    
                    <td>{{ commandes.Quantite  }}</td>
                    <td>{{ commandes.commande.id }}</td>
                    <td>{{ commandes.produit.ProductName }}</td>
                    <td>{{ commandes.Price }}dt</td>
                    
                </tr>
                
            {% endfor %}

            </tbody>

        </table>
        {{ knp_pagination_render(pagination)}}
        <a href="/imprimer">Imprimer</a><a href="/stats">Statistiques</a>
        
                            
    {% endblock %}

