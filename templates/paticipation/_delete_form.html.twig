<form method="post" action="{{ path('app_paticipation_delete', {'id': paticipation.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ paticipation.id) }}">
    <button class="btn">Delete</button>
</form>
